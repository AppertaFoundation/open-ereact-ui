import { styled } from '@material-ui/core/styles';

export const P = styled('p')(() => {
  return {
    fontSize: 1,
    lineHeight: 1.5,
    margin: '0.625rem 0 1.5rem 0',
    color: 'black',
  };
});
